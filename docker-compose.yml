services:
  stremfin:
    build: .
    container_name: stremfin
    env_file:
      - .env
    ports:
      - "3001:3000"
    volumes:
      - stremfin-data:/app/data
    restart: unless-stopped

volumes:
  stremfin-data:
